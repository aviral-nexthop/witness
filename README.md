# witness
witness is a debugging diary for a switch. You type commands in a rec> prompt, it runs them on the box, writes down what happened, then later keeps only the handful of steps that belong in a bug ticket.
